(() => { const list = document.querySelector('#programs-list'); if (!list) return; const safe = value => String(value).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); fetch('data/programs.json').then(r => {if (!r.ok) throw Error(); return r.json();}).then(({programs}) => {if (!programs?.length) {list.innerHTML='<p>No programs have been announced yet. Please check back soon.</p>'; return;} list.innerHTML=programs.map(p=>`<article class="program-row"><img src="${safe(p.image)}" alt=""><div><h2>${safe(p.title)}</h2><p>${safe(p.summary)}</p><p class="details">${safe(p.location)} · ${safe(p.date)}</p></div><span class="tag ${p.status==='upcoming'?'upcoming':''}">${safe(p.status)}</span></article>`).join('');}).catch(()=>{list.innerHTML='<p>Programs could not be loaded. Please refresh or try again later.</p>';});})();
